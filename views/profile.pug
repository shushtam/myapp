extends layout

block content
    .container
        .row
            .col-md-2
            .col-md-8
                .well
                    h2 welcome
                        span
                            a.btn.btn-default.btn-sm(href='/logout') Logout
                    p #{user.name}
                    p #{user.email}

